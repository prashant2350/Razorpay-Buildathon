pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
anthropic>=0.30.0   # optional - use EITHER this OR google-genai for live LLM narration; falls back to offline templates if neither key is set
google-genai>=0.3.0 # optional - Gemini alternative to anthropic; set GEMINI_API_KEY instead of ANTHROPIC_API_KEY to use this
razorpay>=1.4.0     # optional - only needed for real test-mode API calls (data/razorpay_client.py etc.)
python-dotenv>=1.0.0  # optional - convenience for loading .env locally
fastapi>=0.110.0    # for the live demo server (server.py)
uvicorn>=0.27.0     # ASGI server to run server.py
